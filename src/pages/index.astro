---
import PrdEditor from '../components/PrdEditor.astro';
import Base from '../layouts/Base.astro';
import { SITE } from '../lib/site';

const title = `Create a product requirements document · ${SITE.name}`;
---

<Base title={title}>
  <PrdEditor />
</Base>
