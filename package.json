{
  "name": "prd-field-guide",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A practical browser-based template for drafting and downloading product requirements documents.",
  "scripts": {
    "predev": "node scripts/make-mocks.mjs",
    "dev": "astro dev --port 4410 --force",
    "prebuild": "node scripts/make-mocks.mjs",
    "build": "astro build --force",
    "preview": "node scripts/preview.mjs --port 4411",
    "check": "astro check",
    "test": "vitest run && node --test \"scripts/**/*.test.mjs\" \"scripts/check-*.mjs\"",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "notes:missing": "node scripts/history-notes-missing.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource/noto-sans": "^5.3.0",
    "@pdf-lib/fontkit": "^1.1.1",
    "astro": "^5.18.2",
    "docx": "^9.7.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.13.3",
    "jszip": "^3.10.1",
    "sharp": "0.34.5",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
